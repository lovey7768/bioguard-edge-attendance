# Face enrollment samples are stored here.
# This folder is excluded from Git to protect personal biometric data.
# Each enrolled person gets a subfolder: faces/<user_id>/000.jpg, 001.jpg, ...
# Run: python src/enroll.py <user_id> "Full Name"
